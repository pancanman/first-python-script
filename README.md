# My First Python Script

It's a chatbot API wrapper, it uses Gemini API and now contains memory.

# Usage

For usage, you can either replace "api = os.getenv("GEMINI_API_1") with your own Gemini API, or you can make a .env file with your own API key inside of it.


It's very simple and I'm always open to feedback, so if you have anything to tell me let me know. I'm uploading all of my projects as sort of a progress monitor in a way.
